import { __awaiter } from '../node_modules/.pnpm/@rollup_plugin-typescript@12.1.2_rollup@4.34.9_tslib@2.8.1_typescript@5.8.2/node_modules/tslib/tslib.es6.min.js';

class BuryPoint {
    // 构造函数，接收一个参数options，默认值为空对象
    constructor(options = {}) {
        this.defaultKey = options.appId + '_track' || 'default';
        this.eventMap = {};
    }
    // 获取当前埋点数据
    getTrackInfo(el, binding) {
        console.log('binding-getTrackInfo: ', binding);
    }
    // 埋点事件绑定
    handleBindEvent(el, binding) {
        return __awaiter(this, void 0, void 0, function* () {
            if (!binding.value) {
                return;
            }
            this.getTrackInfo(el, binding);
        });
    }
}

export { BuryPoint };
